# Database Configuration
# DATABASE_URL=postgresql://up_crawler:up_crawler@localhost:5432/up_crawler

# Logging
LOG_LEVEL=info

# Puppeteer Configuration
PUPPETEER_HEADLESS=false
PUPPETEER_TIMEOUT=30000
PUPPETEER_USER_DATA_DIR=./user-data

# Upwork Configuration
UPWORK_LOGIN_URL=https://www.upwork.com/ab/account-security/login

# Residential Proxy (Decodo)
PROXY_HOST=us.decodo.com        # country.decodo.com format (e.g., us.decodo.com)
PROXY_PORT=10000                # Not used - auto-selected: 10000=rotating, 10001=sticky(debug)
PROXY_USER=spcecpm8t1           # your session ID (exactly as provided by Decodo)
PROXY_PASS=d+9IX7ujaXdk2yX8qf   # your proxy password (exactly as provided)
PROXY_COUNTRY=us                # country code (e.g., us|uk|sg|ua)
PROXY_ZIP_CODE=94102            # zip code for location targeting
PROXY_ROTATE_MINUTES=10         # sticky duration per run (set manually)

# Debug Mode Credentials (for --debug flag)
DEBUG_EMAIL=your-email@example.com    # email for debug login
DEBUG_PASSWORD=your-password          # password for debug login

# TextVerified.com API Configuration
TEXTVERIFIED_API_KEY=your_api_key
TEXTVERIFIED_EMAIL=your_email
